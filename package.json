{
  "name": "cloudflare-training-2026",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "prepare": "vp config",
    "dev:api": "vp run cloudflare-worker-template#dev",
    "api:db:migrate:local": "vp run cloudflare-worker-template#db:migrate:local"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.2"
}
